import { UserDefinedSafelist } from 'purgecss';

declare const _default: (safeList?: UserDefinedSafelist) => {
    name: string;
    enforce: string;
    transformIndexHtml(html: any): void;
    generateBundle(_options: any, bundle: any): Promise<void>;
};

export { _default as default };
